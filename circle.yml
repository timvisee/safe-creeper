machine:
  java:
    version: oraclejdk8
general:
  artifacts:
    - "target/SafeCreeper-*.jar"
test:
  override:
    - mvn clean install -B
  post:
    - cp ./target/SafeCreeper-*-SNAPSHOT-spigot.jar $CIRCLE_ARTIFACTS
    - cp ./target/SafeCreeper-*-SNAPSHOT-legacy.jar $CIRCLE_ARTIFACTS
    - cp ./target/SafeCreeper-*-SNAPSHOT-spigot.jar $CIRCLE_ARTIFACTS/SafeCreeper.jar
